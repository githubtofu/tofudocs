## access all interface
    $ sudo apt-get install wireshark
    $ sudo dpkg-reconfigure wireshark-common 
    $ sudo usermod -a -G wireshark $USER
    $ newgrp wireshark

## LUA Plugin Directory
    /home/tofuman/.local/lib/wireshark/plugins
    
